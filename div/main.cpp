#include <iostream>
#include <iomanip>

using namespace std;

#define MAX 5000000;
long long m[MAX];

long long f(long long n,long long p,long long q,long long x,long long y)
{
    long long temp;
    if(n<=0)
    {
        return 1;
    }

}
