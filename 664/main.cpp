#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    int n,sum=0,i,b=1,j,m;

    cin >> n >> m;
    int x[n];

    for(i=0;i<n;i++)
    {
        cin >> x[i];
        sum+=x[i]-;
    }

cout <<sum-( m*(sum/100));


}
